version: '3.1'
services:
  casdoor:
    restart: always
    image: casbin/casdoor-all-in-one
    ports:
      - '8000:8000'
    # volumes:
    #   - ./conf:/conf/
    #   - ./conf/init_data.json:/init_data.json